# Fast startup configuration - disables heavy services

# Performance optimizations
app.performance.text-embedding-enabled=false
app.performance.vector-store-enabled=false
app.performance.chatbot-rag-enabled=false

# Log configuration to show what's enabled/disabled
logging.level.local.example.demo.config=INFO 